query GetMatches(
	$filters: MatchFiltersInput
	$pagination: PaginationArg
	$sort: [String]
) {
	matches(filters: $filters, pagination: $pagination, sort: $sort) {
		details {
			... on ComponentSharedTeamScore {
				team {
					name
					logo {
						url
						alternativeText
					}
				}
				score
			}
		}
		date
	}
}
